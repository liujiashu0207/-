# 科研透明看板（反黑盒）

## 0. 目的
这份看板用于让项目全过程可审计、可复查、可复现，避免“黑盒推进”。

> 同步测试标记：2026-03-16 auto-sync test edit by assistant

---

## 1. 当前我已经做了什么（可核对）

### 1.1 文档产出
1. `docs/day3_实验协议_v1.md`
2. `docs/方法章节草稿_v1.md`
3. `docs/实验设计章节草稿_v1.md`
4. `docs/day4_运行命令_并行推进.md`
5. `docs/结果与讨论章节草稿_v1.md`

### 1.2 代码产出
1. `code/planners/core.py`
2. `code/planners/algorithms.py`
3. `code/planners/__init__.py`
4. `code/experiments/run_b_route_experiments.py`

### 1.3 实验结果产出
1. `results/exp_raw_records.csv`
2. `results/exp_summary.csv`
3. `figures/runtime_comparison_b_route.png`

---

## 2. 每一项怎么验（你可直接操作）

### 2.1 验证实验是否真实跑过
在项目根目录执行：

```bash
python code/experiments/run_b_route_experiments.py --sizes 20,40,80,100 --ratios 0.3,0.7 --repeats 30 --seed 42
```

若控制台输出以下三行，则说明实验流程真实执行：
- Raw saved: `results/exp_raw_records.csv`
- Summary saved: `results/exp_summary.csv`
- Figure saved: `figures/runtime_comparison_b_route.png`

### 2.2 验证数据不是“编的”
打开 `results/exp_summary.csv`，检查以下点：
1. 是否包含 5 个算法：`dijkstra, astar, weighted_astar, jps_like, improved_astar`
2. 是否包含 8 个场景组合：`20/40/80/100` x `0.3/0.7`
3. 是否每组都有 `runtime_ms_mean, path_length_mean, turn_count_mean`
4. `improved_astar` 在大多数场景是否呈现“路径更短 + 转弯更少”

---

## 3. 关键结果（当前版本）

### 3.1 低障碍大地图（100x100, ratio=0.3）
- A*：runtime `1.5559 ms`，path `63.2640`，turn `20.47`
- 改进A*：runtime `0.6532 ms`，path `60.2467`，turn `13.37`
- 结论：改进A*在该组同时提升速度与路径质量

### 3.2 高障碍大地图（100x100, ratio=0.7）
- A*：path `8.4340`，turn `3.87`
- 改进A*：path `7.2827`，turn `2.40`
- 结论：改进A*路径质量更优；时间不一定最优（weighted_astar 可能更快）

---

## 4. 我承认的局限（避免“乱搞”）
1. 当前 `jps_like` 是轻量实现，不是完整论文级双向JPS。  
2. 当前结果主要是静态栅格场景，动态障碍实验还未展开。  
3. 当前尚未加入显著性检验脚本（t检验），需要补齐。

---

## 5. 从现在开始的透明推进规则（立即生效）
1. 每次只推进一个小步骤，不再连续“快进”。  
2. 每步固定输出 5 项：
   - 为什么做这步
   - 改了哪些文件
   - 执行了什么命令
   - 得到了什么结果
   - 存在什么风险
3. 每步结束后先给你确认，你同意再进入下一步。  
4. 任何关键结论必须给出对应文件路径与可复查命令。

---

## 6. 下一步候选（你确认后再做）
1. 补“显著性检验脚本”（把结果从“有提升”变成“统计显著提升”）  
2. 把方法章节改为“严格对应已实现代码版本”（减少论文与代码偏差）  
3. 做一页“论文证据总表”（每个结论对应哪组数据）
