# 数据真实性审计（第1轮）

## 审计范围
- 原始数据：`results/exp_raw_records.csv`
- 汇总数据：`results/exp_summary.csv`

## 审计命令
```bash
python -c "import pandas as pd; from pathlib import Path; root=Path(r'c:\Users\33839\Desktop\科研论文'); raw=pd.read_csv(root/'results'/'exp_raw_records.csv'); sm=pd.read_csv(root/'results'/'exp_summary.csv'); ..."
```

## 核验结果（客观）
1. `exp_raw_records.csv` 行数：`1035`  
2. 理论期望原始行数：`1200`（4尺寸 x 2障碍率 x 30轮 x 5算法）  
3. 缺失原始行数：`165`  
4. 原始数据空值数：`0`  
5. 汇总行数：`40`，与期望一致（4 x 2 x 5）  
6. 汇总数据空值数：`0`  
7. 缺失组数量：`0`（每个 size-ratio-algo 组合都有数据）  
8. 成功率范围：`[1, 1]`

## 结论
- 数据不是“伪造”：文件结构完整，分组齐全，成功率一致。  
- 但当前并非严格“每组30次全满样本”：原始行数少于理论值。  

## 原因定位（已确认）
当前脚本在某些随机地图上无法在限定重试次数内采到“可达起终点”，会跳过该轮，因此实际有效轮次低于 30。

## 风险等级
- 风险等级：**中等**  
- 影响：会弱化“固定30次重复实验”的严格性，投稿时可能被审稿人追问样本一致性。

## 修正建议（下一步）
1. 将每个场景改为“直到采满30个有效样本”为止（强制满样本）。  
2. 在原始表新增 `attempt_id` 和 `valid_sample_id`，完整记录采样过程。  
3. 汇总表新增 `effective_repeats` 字段，公开每组真实样本数。
